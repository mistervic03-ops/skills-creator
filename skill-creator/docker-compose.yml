services:
  backend:
    build:
      context: ./backend
    env_file:
      - .env
    expose:
      - "8000"

  frontend:
    build:
      context: ./frontend
    depends_on:
      - backend
    ports:
      - "80:80"
